import { QuizFlow } from "@/components/funnel/QuizFlow";

// Plain server wrapper around the client-side funnel; route metadata arrives
// in Task 7.
export default function Page() {
  return <QuizFlow />;
}
