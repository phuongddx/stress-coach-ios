---
task: 260811-t0l
type: quick
autonomous: true
files_modified:
  - .github/workflows/_test.yml
---

<objective>
Fix the `test` job in `.github/workflows/_test.yml` so it no longer hardcodes a simulator that doesn't exist on the current `macos-15-arm64`/Xcode 26.3 runner image. The "Run Tests" step currently targets `-destination 'platform=iOS Simulator,name=iPhone 16,OS=latest'`, which fails with "device not found" (confirmed via GitHub Actions run 31493553791, job 93785581970).

Purpose: Restore a passing CI test job by resolving an available iPhone Simulator UDID dynamically at runtime, instead of pinning to a device name that may not exist on a given runner image.
Output: Updated `.github/workflows/_test.yml` — `test` job only. `lint-and-build`, `build-watchos`, `build-widget` jobs untouched.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Existing resilience pattern already in this file (`build-watchos` job, lines ~92-96 and `build-widget` job, lines ~123-128): both check `xcodebuild -showdestinations ... | grep -q "<platform substring>"` before an operation, and download the platform if the check fails. The `test` job fix below follows the same "detect via xcrun/xcodebuild output, fail loud with a full dump if nothing usable is found" spirit, adapted to resolving a concrete simulator UDID rather than a platform substring.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dynamic simulator resolution step and rewire Run Tests destination</name>
  <files>.github/workflows/_test.yml</files>
  <action>
In the `test` job only (starts at the `test:` key, currently lines 143-208), insert a new step immediately before the existing "Run Tests" step (after the "Cache SPM Packages" step). Name it "Resolve available iPhone Simulator", give it `id: simulator`, and have it run:

- `xcrun simctl list devices available` piped through `grep` for lines containing `iPhone`, taking the first match.
- If no matching line is found: print an error to stderr, dump the full `xcrun simctl list devices available` output to stderr (loud-failure, mirroring the existing `-showdestinations`-check-then-act pattern in `build-watchos`/`build-widget`), and `exit 1`.
- Otherwise, extract the UDID (the `XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX` token in parentheses) from the matched line via `grep -oE` with the standard UUID pattern, and write it to `$GITHUB_OUTPUT` as `udid=<value>` so the next step can read `steps.simulator.outputs.udid`.
- If UDID extraction itself fails (empty result), also print an error and `exit 1` rather than proceeding with an empty destination.
- Use `set -o pipefail` at the top of the run block, matching every other multi-command step in this file.

Then, in the existing "Run Tests" step's `xcodebuild test` invocation, change the `-destination` argument from the single-quoted literal `'platform=iOS Simulator,name=iPhone 16,OS=latest'` to the double-quoted, output-interpolated `"platform=iOS Simulator,id=${{ steps.simulator.outputs.udid }}"`. Do not change any other flag on that `xcodebuild test` command (keep `-resultBundlePath`, `-parallel-testing-enabled NO`, `-maximum-concurrent-test-simulator-destinations 1`, `CODE_SIGN_IDENTITY=""`, `CODE_SIGNING_REQUIRED=NO`, and the `tee`/`xcpretty` pipe exactly as-is).

Do not touch the `lint-and-build` job's `generic/platform=iOS Simulator` destination (build-only, no booted device needed) or any other job in this file.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/_test.yml')); steps=d['jobs']['test']['steps']; names=[s.get('name') for s in steps]; assert 'Resolve available iPhone Simulator' in names, names; i=names.index('Resolve available iPhone Simulator'); assert names[i+1]=='Run Tests', names; assert steps[i].get('id')=='simulator', steps[i]; run=steps[i+1]['run']; assert 'steps.simulator.outputs.udid' in run and 'name=iPhone 16' not in run, run; print('OK')"</automated>
  </verify>
  <done>`.github/workflows/_test.yml` parses as valid YAML; the `test` job has a "Resolve available iPhone Simulator" step (id `simulator`) immediately before "Run Tests"; "Run Tests" no longer references `name=iPhone 16` and instead uses `platform=iOS Simulator,id=${{ steps.simulator.outputs.udid }}`; `lint-and-build`/`build-watchos`/`build-widget` jobs are byte-identical to before this change.</done>
</task>

</tasks>

<verification>
1. `git diff .github/workflows/_test.yml` shows changes scoped entirely to the `test` job (new step + one modified `-destination` line).
2. YAML re-parses cleanly (see Task 1 `<verify>`).
3. Manual read-through: the new step's failure path dumps the full device list before exiting non-zero, so a future "no iPhone simulator available" runner failure is diagnosable from CI logs rather than a bare "device not found".
</verification>

<success_criteria>
- `test` job no longer hardcodes `name=iPhone 16,OS=latest`.
- Simulator UDID is resolved at runtime from `xcrun simctl list devices available` and passed via step output.
- No other job in `.github/workflows/_test.yml` is modified.
- Change is CI-only YAML; no local build/test executes it, so verification is diff review + YAML syntax validation.
</success_criteria>

<output>
Create `.planning/quick/260811-t0l-fix-ci-failure-in-github-workflows-test-/260811-t0l-SUMMARY.md` when done.
</output>
